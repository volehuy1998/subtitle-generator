name: Docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "**.md"
  pull_request:
    branches: [main]
    paths:
      - "docs/**"
      - "**.md"

jobs:
  Lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docs-only change -- lint skipped"

  Test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docs-only change -- test skipped"

  Frontend-Tests:
    name: Frontend Tests
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docs-only change -- frontend tests skipped"

  Build-Docker:
    name: Build Docker
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docs-only change -- docker build skipped"
